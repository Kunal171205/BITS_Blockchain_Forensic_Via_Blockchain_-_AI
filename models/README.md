# ML Models Directory

This directory is intended to store the downloaded `.pkl` models from your Kaggle/Colab notebook environments.

> **Important**:
> Please place your `unet_tamper_model.pkl` file in this exact folder (`models/`) so that `src/ml_inference.py` can load it for real-world document verification.
